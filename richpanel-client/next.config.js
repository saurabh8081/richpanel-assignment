/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
      domains: ['cdn.unstop.com', 'd8it4huxumps7.cloudfront.net'],
  },
}

module.exports = nextConfig;
